# 图12：模块11_接口与画面模块实现图

## 1. 图示
```mermaid
flowchart TD
    A["0093_createRuntime_创建运行时逻辑"] --> B["注册 REST 路由 /v1/proxies/*"]
    A --> C["注册页面路由 /proxy-admin /runtime/logs"]
    A --> D["注册 SSE 路由 /api/runtime/logs/stream /api/runtime/thread-pools/stream"]

    C --> E["0131_renderProxyAdminPage_渲染代理管理页面逻辑"]
    C --> F["0132_renderRuntimeLogsPage_渲染运行时日志页面逻辑"]

    G["logger.subscribe"] --> H["0091_sendSse_发送SSE逻辑"]
    I["workerPool.subscribe"] --> H
    J["engine.on(snapshot)"] --> H

    K["proxy-admin.html 前端"] --> L["轮询 /v1/proxies/pool-status/list/events/..."]
    K --> M["订阅 /api/runtime/thread-pools/stream"]
    N["runtime-logs.html 前端"] --> O["拉取 /v1/runtime/logs"]
    N --> P["订阅 /api/runtime/logs/stream"]
```

## 2. 中文讲解
1. 接口模块在 `0093_createRuntime_创建运行时逻辑` 中完成路由挂载，分为 REST、页面和 SSE 三类。
2. 页面渲染入口分别是 `0131_renderProxyAdminPage_渲染代理管理页面逻辑` 与 `0132_renderRuntimeLogsPage_渲染运行时日志页面逻辑`。
3. SSE 统一通过 `0091_sendSse_发送SSE逻辑` 发包，消息来源有三路：运行日志、线程池状态、引擎快照。
4. `/proxy-admin` 页面采用“轮询 + SSE”混合策略，轮询拉取全量看板，SSE补充实时线程池变化。
5. `/runtime/logs` 页面采用“初始加载 + SSE增量”策略，先拉历史日志再持续插入新日志。
6. 接口模块不计算业务规则，只负责聚合现有状态并对外暴露，这样更稳定、可替换性更高。

